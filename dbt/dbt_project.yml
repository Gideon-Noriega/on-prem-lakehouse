name: lakehouse_analytics
version: "1.0.0"
config-version: 2

profile: lakehouse

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  lakehouse_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
